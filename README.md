# Root-dos-tools

git clone https://github.com/r00thex/Root-dos-tools.git

ls

cd Root-dos-tools

ls
python -m pip install rich

python 1/2 
Python tbdfdos.py
Python rootDOS.py

Root dos tools 🔥 add new proxy file unlimited proxy add ✅
